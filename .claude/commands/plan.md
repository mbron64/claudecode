---
description: Explore the relevant code and produce a concrete implementation plan before any edits.
allowed-tools: Read, Grep, Glob, Bash(git diff *), Bash(git log *)
---

You are in PLAN mode. Do not edit files yet.

For the task: **$ARGUMENTS**

1. Explore the relevant code. Identify the files involved and how they fit together.
2. Produce a plan with:
   - **Files to change** and the change in one line each.
   - **Tests to add or update** (path + the branches/cases they cover).
   - **Risks / unknowns** and how you'll de-risk them.
   - **Verification:** the exact commands you'll run (default `npm run verify`).
3. Keep it tight. No code yet — wait to switch into auto mode to implement.
